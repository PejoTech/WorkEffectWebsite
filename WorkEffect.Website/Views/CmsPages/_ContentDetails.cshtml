@using WorkEffect.Website.Models
@model WorkEffect.Website.Types.CmsContent

<div class="row">
    <div class="col-lg-11">
        @Html.Partial("_render", new RenderModel { Content = Model.Content })
    </div>
    <div class="col-lg-1">
        <a href="@Url.Action("DeleteContent", "CmsPages", new { id = Model.Id, pageId = Model.CmsPageId })">
            <i class="glyphicon glyphicon-remove"></i>
            <span class="sr-only">Edit</span>
        </a>
    </div>
</div>